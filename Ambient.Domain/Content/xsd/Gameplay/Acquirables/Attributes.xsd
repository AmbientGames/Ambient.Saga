<?xml version="1.0" encoding="utf-8"?>
<xs:schema xmlns:xs="http://www.w3.org/2001/XMLSchema" targetNamespace="Ambient.Domain" xmlns="Ambient.Domain" elementFormDefault="qualified">
    <xs:include schemaLocation="../Actors/CharacterBase/CharacterStatsAttributes.xsd" />
    <!--
    Attributes: A stat block that can be applied to characters, equipment, spells, consumables.
    Contains resources, state, and attribute modifiers.
    -->
    <xs:complexType name="Attributes">
        <xs:attributeGroup ref="StatBlock" />
    </xs:complexType>
</xs:schema>
